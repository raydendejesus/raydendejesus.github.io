# Keyframe Animation Maker
Add keyframes with transform/opacity values, set duration/easing/iterations, preview, and copy the @keyframes + animation CSS.
Single self-contained index.html — open by double-clicking.

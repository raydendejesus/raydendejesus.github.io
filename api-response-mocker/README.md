# api-response-mocker

Define typed fields, a status code, and method to build a mock REST JSON response (single or array); copy body or full HTTP response.
Single self-contained `index.html` — double-click to run.

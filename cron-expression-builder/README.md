# cron-expression-builder

Edit the five cron fields (or pick a preset) to build a cron string with a live plain-English description. Copy it.
Single self-contained `index.html` — double-click to run.

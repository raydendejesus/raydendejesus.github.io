# placeholder-image-generator

Generate a placeholder image at any size with custom colors and text; live canvas preview, download as PNG or SVG.
Single self-contained `index.html` — double-click to run.

# QR Code Maker

Generate scannable QR codes from text or URLs entirely offline (built-in Reed-Solomon encoder, no libraries). Pick an error-correction level and download the result as a PNG. Single self-contained `index.html`.
